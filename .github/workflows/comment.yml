name: Comment on PR
on: check_run
  
jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Dump Github context        
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: | 
          echo "$GITHUB_CONTEXT"
          echo github.event
